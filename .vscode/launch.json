{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Start Dev Server (yarn dev)",
      "request": "launch",
      "command": "yarn dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}